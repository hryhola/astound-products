<div class="card master-product m-2">
    <img class="card-img-top" src={{image}} alt="product">
    <div class="card-body">
        <h5 class="card-title">{{name}}</h5>
        <p class="card-text">{{price}}</p>
        <a href="#" class="btn btn-primary">Add to basket</a>
    </div>
</div>
